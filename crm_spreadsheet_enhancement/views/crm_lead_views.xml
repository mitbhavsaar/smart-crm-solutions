<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_crm_lead_form_inherit_custom" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_schedule_meeting']" position="after">
                <button
                    name="action_open_lead_spreadsheet"
                    class="oe_stat_button"
                    type="object"
                    icon="fa-calculator"
                    invisible = "type != 'opportunity' or not template_id"
                    groups="crm_spreadsheet_enhancement.group_cost_calculator"
                    >
                    <div class="o_stat_info">
                        <span class="o_stat_text">Cost Calculator</span>
                        <span class="o_stat_value">
                            <field name="quote_calculator_id" nolabel="1" readonly="1"/>
                        </span>
                    </div>
                </button>
            </xpath>
            <xpath expr="//field[@name='tag_ids']" position="after">
                <field name="template_id" invisible="1"/>
            </xpath> 
            <!-- <xpath expr="//field[@name='material_line_ids']/list" position="inside">
                <field name="price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                <field name ="currency_id" invisible = "1"/>
            </xpath> -->
 
        </field>
    </record>
</odoo>
