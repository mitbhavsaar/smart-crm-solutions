<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Add "Required File?" field to product attribute values list -->
    <record id="view_product_attribute_form_inherit_required_file" model="ir.ui.view">
        <field name="name">product.attribute.form.inherit.required.file</field>
        <field name="model">product.attribute</field>
        <field name="inherit_id" ref="product.product_attribute_view_form"/>
        <field name="arch" type="xml">
            <!-- Add Required File? field after is_custom in the attribute values list -->
            <!-- <xpath expr="//field[@name='value_ids']//field[@name='is_custom']" position="after">
                <field name="required_file" 
                       string="Required File?"
                       column_invisible="parent.display_type not in ['radio', 'select']"/>
            </xpath> -->
        </field>
    </record>
</odoo>
